name: Deploy
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v3
        with:
          node-version: 18.10
      - run: |
          npm install
          npm start
